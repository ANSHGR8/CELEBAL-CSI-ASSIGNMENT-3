import React from 'react';

const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      {/* Add dashboard components like summary cards, charts, etc. */}
    </div>
  );
};

export default Dashboard;
